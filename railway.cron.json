{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements-web.txt"
  },
  "deploy": {
    "startCommand": "python -m web.app.services.cron",
    "restartPolicyType": "NEVER"
  }
}
